- provide(:title, @user.name)
.row
  %aside.span4
    %section
      %h3= @user.name
      = @user.email
  .span8
    - if @user.initiatives.any?
      %h3= "Initiatives (#{@user.initiatives.count})"
      %ol.initiatives
        = render @initiatives
      = will_paginate @initiatives